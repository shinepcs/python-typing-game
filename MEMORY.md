# PyType Arena 구현 메모

- React 19 개발 모드의 이중 마운트를 고려하여 Babylon `Engine` 초기화는 컴포넌트 ref로 보호한다.
- 타이핑은 영문 코드 중심이다. 조합형 한글 IME 입력을 게임 입력으로 취급하지 않으며, 문서·UI에는 한국어를 사용한다.
- 모든 게임 입력은 전역 키보드 이벤트에서 받고, 브라우저 단축키에는 간섭하지 않는다.
- 수치 계산은 화면 렌더와 분리된 `TypingArena` 클래스가 담당한다.
- `?demo`는 키보드 없이 일정 속도로 정확한 글자를 입력해 시각 검토 상태를 재현한다.
