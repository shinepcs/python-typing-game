# Game Plan: PyType Arena

## Risk Tasks

### 1. 실시간 키보드 판정 및 타이머 상태
- **Why isolated:** 코드 입력은 스페이스·줄바꿈·대소문자까지 정확히 판정해야 하며, 시작·일시정지·완료 상태에서 시간 및 점수 계산이 중복되면 기록이 왜곡될 수 있다.
- **Approach:** 키보드 이벤트를 단일 `TypingArena` 상태 객체에서 수집하고, 현재 문자 위치·오류 수·정확 입력 수·시작 시각·남은 시간을 명시적 상태로 관리한다. 특수 키는 별도 처리하고, 브라우저 기본 단축키와 조합 입력은 편집 대상이 아닐 때만 예방한다.
- **Verify:** 영문·공백·개행·백스페이스 입력이 목표 코드와 일치할 때만 진행되고, 오류 후 같은 위치의 재입력이 가능하며, 타이머가 시작·일시정지·재시작·완료 사이에서 중복 감산되지 않는다.

## Main Build

웹 기반 파이썬 타자 아레나를 만든다. 사용자는 난이도를 선택하고 `for` 루프·함수·리스트 컴프리헨션 등의 짧은 파이썬 구문을 타이핑한다. 대시보드는 남은 시간, 정확도, WPM, 콤보 및 XP 진행을 즉시 표시하며, 완료 시 결과와 다음 미션 제안을 보여 준다.

- **Assets:**
  - `pytype-arena-command-deck` 배경 아트 — 1920×1080, 전체 게임 배경의 저대비 질감.
  - `pytype-arena-visual-target` HUD 레퍼런스 — 16:9, 미션 레일의 상태 프리뷰 장식.
  - `pytype-arena-streak-badge` 배지 — 160×160 px, 연속 기록 패널.
  - `pytype-arena-logo` 투명 심벌 — 헤더·파비콘·시작 패널, 48×48 px 이상.
- **Verify:**
  - 데스크톱과 모바일에서 코드·계기판·컨트롤이 겹치거나 잘리지 않는다.
  - 정확한 입력, 오입력, 백스페이스, 다시 시작, 난이도 전환, 타이머 종료가 눈에 보이는 상태로 작동한다.
  - 결과 오버레이가 WPM·정확도·정확 입력 수를 표시하고 “다음 코드”와 “다시 도전”을 제공한다.
  - 이미지 및 아이콘이 누락되지 않으며, 화면에 테스트 데이터나 기본 플레이스홀더가 남지 않는다.
  - 브라우저 콘솔 오류가 없고, 시각 언어는 레퍼런스의 흑청색·라임 신호·조종석 밀도를 따른다.
  - `?demo`에서는 타이핑 진행 및 계기판 변화가 결정적으로 나타나 스크린샷으로 확인 가능하다.
